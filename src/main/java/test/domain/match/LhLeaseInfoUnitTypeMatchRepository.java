package test.domain.match;

import org.springframework.data.jpa.repository.JpaRepository;

public interface LhLeaseInfoUnitTypeMatchRepository extends JpaRepository<LhLeaseInfoUnitTypeMatch, Long> {
}
